/**
 * This file just re-exports everything from the main config
 * to make the import syntax a bit simpler in other files
 * 
 * @author Mudbill
 */

export * from './main.config';
export { default } from './main.config';